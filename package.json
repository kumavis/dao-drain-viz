{
  "name": "dao-drain-viz",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && cp e/* dist/ && browserify e/index.js -o dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^2.0.0-rc.6",
    "d3": "^3.5.17",
    "raphael": "^2.2.0",
    "raphael-browserify": "^2.1.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0"
  }
}
